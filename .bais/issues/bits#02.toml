id = "bits#02"
title = "BITS self-file: chain-blocks ready-nonexact"
status = "Done"
kind = "Bug"
area = "bits/t2"
body = """
BITS self-file: arm chain-blocks graded Fail (ready-nonexact).

Suite: drill(bits-t2). Filed by the suite, not by hand — the suite
uses the tool to prove the tool.

Transcript:
{
  "ready": [
    {
      "issue": {
        "id": "t#01",
        "title": "unblock me first",
        "status": "Open",
        "kind": "Feat",
        "area": null,
        "severity": null,
        "source": null,
        "body": "Head of the chain. Closing t#01 must surface t#02 in ready.\n"
      },
      "edges": [],
      "holder": null,
      "lease": null
    }
  ],
  "unparseable": []
}

ready=["t#01"]

Drill provenance (hub#153, 2026-09-06): this filing is the acceptance
proof, not a substrate bug. The chain-blocks expectation in bits-t2.mjs
was deliberately sabotaged (expected [t#02] pre-move); the suite failed
FOR THAT REASON, filed this issue with the transcript above, then the
hunk was restored and the suite re-ran 6/6 green. Kept Open as the
self-filing loop's existence proof.

Diagnosis (kimi-bits-selffile, 2026-09-07): the mismatch was in the
derivation, not the ready set. The transcript above records only the
ACTUAL side of the comparison (ready=["t#01"], correct-looking) while
the EXPECTED side lived only in the runner source — a ready-nonexact
filing that could not explain itself. The post-move check also derived
ready-nonempty for an exact comparison (the opposite of what an
un-surfaced tail means), and self_file_body() in main.baml had drifted
from the body the host actually wrote (plus a stale header comment
claiming ready-nonempty/bits#03 for this drill). Fix: both chain-blocks
comparisons derive ready-nonexact with ready=<actual>
expected=<expected> in the note; self_file_title()/self_file_body() are
the typed shape scripts/bits-t2.mjs mirrors byte-for-byte, pinned by a
T0 literal; header comment corrected. Red-checked per bi#57 on the
fixed comparison (record in bits-t2.mjs header).

Evidence: drill(bits-t2)
"""
